using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class EnemyAttack : MonoBehaviour
{

    /*public string[] skillList;

    private void ObtainPossibleAttackCells(TerrainObjects terrainUnder){
        List<TerrainObjects> reachableCells = new List<TerrainObjects>();

        foreach(string skill in skillList){
            if(skill == "firewall"){
                Helper.GetSurroundingCoords(3, terrainUnder, terrainObjects).ToArray();
            }
        }
    }

    public void DetectSurrounder(){
        TerrainObjects playerPosID = Helper.GetActualPlayerBlockFromGameObject("Player", false, Constants.terrainObjects);
        ObtainPossibleAttackCells(playerPosID);
    }*/
}
